@model MVCPetAdoption.ViewModels.ShoppingCartViewModel

@{
    ViewBag.Title = "Shopping Cart";
}
<script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.5.1.min.js" type="text/javascript"></script>
<link type="text/css" rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css"/>
<script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
<script type="text/javascript">

    $(function () {
       
        $('#MyDate').datepicker();

        $('.button>#error').hide();
        $('.button>a').click(function (e) {
            if ($('.date').val() == null || $('.date').val() == "") {
                $('#error').show();
                e.preventDefault();
            } else {
                $('#error').hide();
                console.log($('.date').val())
            }
        });

        $('.date').change(function () {
            $('#error').hide();
        });

        // Document.ready -> link up remove event handler
        $(".RemoveLink").click(function () {
            // Get the id from the link
            var recordToDelete = $(this).attr("data-id");

            if (recordToDelete != '') {

                // Perform the ajax post
                $.post("/ShoppingCart/RemoveFromCart", { "id": recordToDelete },
                    function (data) {
                        // Successful requests get here
                        // Update the page elements
                        if (data.ItemCount == 0) {
                            $('#row-' + data.DeleteId).fadeOut('slow');
                        } else {
                            $('#item-count-' + data.DeleteId).text(data.ItemCount);
                        }

                        //$('#cart-total').text(data.CartTotal);
                        $('#update-message').text(data.Message);
                        $('#cart-status').text('Cart (' + data.CartCount + ')');
                    });
            }
        });

    });


    function handleUpdate() {
        // Load and deserialize the returned JSON data
        var json = context.get_data();
        var data = Sys.Serialization.JavaScriptSerializer.deserialize(json);

        // Update the page elements
        if (data.ItemCount == 0) {
            $('#row-' + data.DeleteId).fadeOut('slow');
        } else {
            $('#item-count-' + data.DeleteId).text(data.ItemCount);
        }

        $('#cart-total').text(data.CartTotal);
        $('#update-message').text(data.Message);
        $('#cart-status').text('Cart (' + data.CartCount + ')');
        
    }
</script>

<h3>
    <em>Review</em> your wishlist:
</h3>
<p class="button">
    @Html.ActionLink("Checkout>>", "Complete", "Checkout")
    <br />
    <label id="error" class="label label-danger" hidden>*Please select a pickup date first.</label>
</p>
<div id="update-message">
</div>
<table>
    <tr>
        <th>
           Your future Pet!

        </th>
           
        <th></th>
    </tr>
    @foreach (var item in
Model.CartItems)
    {
        <tr id="row-@item.RecordId">
            <td>
                @Html.ActionLink(item.Pet.Name, "Details", "Store", new { id = item.PetId }, null)
            </td>
        
            @*<td>
                @item.Album.Price
            </td>*@
          
            <td>
                <a href="#" class="RemoveLink"
                   data-id="@item.RecordId">
                    Remove
                    from WishList
                </a>
            </td>
        </tr>
    }
    <tr>

        @*<td>
            Total
        </td>*@
        <td></td>
        <td></td>
        @*<td id="cart-total">
            @Model.CartTotal
        </td>*@
    </tr>
</table>
<div class="form-group">
    @*@Html.Label(" Select a date to pick up your pets")
    <br />
    @Html.TextBox("MyDate")*@
    @Html.Label("MyDate", "Pick Up Date")
    @Html.TextBox("MyDate", null, new { placeholder = "Enter Date to Pick Up Pet", required = "required", @class = "form-control date"})
</div>
